

<% @movies.each do |movie| %>
<p><%= link_to movie.title, movie_path(movie) %></p>

<% if params[:search].nil?%>
  <%= render "indexform" %>
  <%= render "searchform" %>
<% else %>
  <%= render "searchresult" %>

<% end %>

<div id="app"></div>
